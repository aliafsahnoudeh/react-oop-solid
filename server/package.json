{
  "name": "time-selection-server",
  "version": "1.0.0",
  "description": "a fake RESTful server to provide fake data!",
  "author": "aliafsahnoudeh",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "json-server --watch time_slots.json --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
